# ⚡ Cheat Sheet React - Referencia rápida\n\nCopia/pega y adapta según necesites.\n\n---\n\n## 📦 Imports\n\n```javascript\n// Lo básico\nimport React from 'react';\nimport { useState } from 'react';\nimport { useEffect } from 'react';\nimport { useRef } from 'react';\n\n// Todo en uno\nimport { useState, useEffect, useRef } from 'react';\n```\n\n---\n\n## 🔧 useState - Manejar estado\n\n### Simple\n```javascript\nconst [nombre, setNombre] = useState('inicial');\n\n// Usar\nsetNombre('nuevo valor');\n\n// En JSX\n<p>{nombre}</p>\n<input value={nombre} onChange={(e) => setNombre(e.target.value)} />\n```\n\n### Array\n```javascript\nconst [peliculas, setPeliculas] = useState([]);\n\n// Agregar\nsetPeliculas([...peliculas, nuevaPelicula]);\n\n// Eliminar\nsetPeliculas(peliculas.filter(p => p.id !== id));\n\n// Actualizar\nsetPeliculas(peliculas.map(p => \n  p.id === id ? { ...p, titulo: 'nuevo' } : p\n));\n```\n\n### Objeto\n```javascript\nconst [user, setUser] = useState({ nombre: '', edad: 0 });\n\n// Actualizar\nsetUser({ ...user, nombre: 'Juan' });\n```\n\n---\n\n## 🌐 fetch - Llamar APIs\n\n### GET simple\n```javascript\nconst datos = await fetch(url);\nconst json = await datos.json();\n```\n\n### Con try/catch\n```javascript\ntry {\n  const respuesta = await fetch(url);\n  if (!respuesta.ok) throw new Error('Error');\n  const datos = await respuesta.json();\n  console.log(datos);\n} catch (error) {\n  console.error('Error:', error);\n}\n```\n\n### Con template string\n```javascript\nconst query = 'Inception';\nconst url = `https://api.example.com?search=${query}`;\n```\n\n### TMDB (nuestra API)\n```javascript\nconst apiKey = 'TU_KEY';\nconst url = `https://api.themoviedb.org/3/search/movie?api_key=${apiKey}&query=${query}&language=es-ES`;\nconst datos = await fetch(url).then(r => r.json());\nconsole.log(datos.results);\n```\n\n---\n\n## 🔄 useEffect - Ejecutar código\n\n### Solo al montar\n```javascript\nuseEffect(() => {\n  console.log('Componente cargado');\n}, []); // Array vacío = una sola vez\n```\n\n### Cada vez que cambia un estado\n```javascript\nuseEffect(() => {\n  console.log('Consulta cambió:', consulta);\n}, [consulta]); // Dependencia\n```\n\n### Cleanup (desmontar)\n```javascript\nuseEffect(() => {\n  const timer = setTimeout(() => {}, 1000);\n  \n  return () => clearTimeout(timer);\n}, []);\n```\n\n---\n\n## 📍 useRef - Acceso al DOM\n\n### Elemento HTML\n```javascript\nconst inputRef = useRef(null);\n\n// En useEffect\nuseEffect(() => {\n  inputRef.current.focus();\n}, []);\n\n// En JSX\n<input ref={inputRef} />\n```\n\n### Dialog (modal)\n```javascript\nconst dialogRef = useRef(null);\n\nuseEffect(() => {\n  if (abierto && dialogRef.current) {\n    dialogRef.current.showModal();\n  } else if (!abierto && dialogRef.current) {\n    dialogRef.current.close();\n  }\n}, [abierto]);\n\nreturn <dialog ref={dialogRef}>{...}</dialog>;\n```\n\n---\n\n## 🎁 Props - Pasar datos\n\n### Pasar\n```javascript\n<MiComponente titulo=\"Hola\" numero={5} handler={miFunc} />\n```\n\n### Recibir (desestructurado)\n```javascript\nexport default function MiComponente({ titulo, numero, handler }) {\n  return (\n    <div>\n      <h1>{titulo}</h1>\n      <p>{numero}</p>\n      <button onClick={handler}>Click</button>\n    </div>\n  );\n}\n```\n\n### Defaultear props\n```javascript\nfunction MiComponente({ titulo = 'Sin título', handler = () => {} }) {\n  return <h1>{titulo}</h1>;\n}\n```\n\n---\n\n## 🎨 .map() - Renderizar listas\n\n### Básico\n```javascript\n{items.map(item => <div>{item}</div>)}\n```\n\n### Con key (IMPORTANTE)\n```javascript\n{items.map(item => (\n  <div key={item.id}>{item.title}</div>\n))}\n```\n\n### Componente\n```javascript\n{peliculas.map(pelicula => (\n  <PolaroidCard \n    key={pelicula.id} \n    item={pelicula}\n    onSelect={handleSelect}\n  />\n))}\n```\n\n---\n\n## 🎯 Eventos\n\n### Click\n```javascript\n<button onClick={() => console.log('click')}>\n  Click\n</button>\n```\n\n### Input\n```javascript\n<input \n  value={valor}\n  onChange={(e) => setValor(e.target.value)}\n/>\n```\n\n### Select\n```javascript\n<select value={opcion} onChange={(e) => setOpcion(e.target.value)}>\n  <option value=\"a\">Opción A</option>\n  <option value=\"b\">Opción B</option>\n</select>\n```\n\n### Form\n```javascript\n<form onSubmit={(e) => {\n  e.preventDefault();\n  handleSubmit();\n}}>\n  <input />\n  <button type=\"submit\">Enviar</button>\n</form>\n```\n\n---\n\n## 🏗️ Estructura de componente\n\n```javascript\nimport { useState, useEffect, useRef } from 'react';\n\nexport default function MiComponente({ prop1, prop2 }) {\n  // 1. Estados\n  const [estado, setEstado] = useState('inicial');\n  \n  // 2. Refs\n  const ref = useRef(null);\n  \n  // 3. Effects\n  useEffect(() => {\n    // Código que se ejecuta\n  }, []);\n  \n  // 4. Funciones\n  const manejarClick = () => {\n    setEstado('nuevo');\n  };\n  \n  // 5. Return (JSX)\n  return (\n    <div>\n      <p>{estado}</p>\n      <button onClick={manejarClick}>Click</button>\n    </div>\n  );\n}\n```\n\n---\n\n## 🧮 Expresiones útiles\n\n### Condicional ternario\n```javascript\n{logueado ? <p>Bienvenido</p> : <p>Login</p>}\n```\n\n### AND lógico\n```javascript\n{items.length > 0 && <p>Tienes {items.length} items</p>}\n```\n\n### Operador opcional\n```javascript\nconst titulo = pelicula?.title || 'Sin título';\n```\n\n### Spread operator\n```javascript\nconst nuevo = [...array, item];\nconst obj2 = { ...obj, propiedad: 'nuevo' };\n```\n\n---\n\n## 🐛 Debug\n\n### Ver en consola\n```javascript\nconsole.log('valor:', valor);\nconsole.log('datos completos:', miObj);\nconsole.table(array); // Tabla\nconsole.error('Esto es un error');\n```\n\n### Debugger\n```javascript\ndebugger; // Pausa aquí en DevTools\n```\n\n### React DevTools\n- F12 → Components\n- Ver estado de componentes\n- Inspeccionar props\n\n---\n\n## ✅ Checklist antes de \"funciona\"\n\n- [ ] ¿Importé todos los hooks que uso?\n- [ ] ¿Guardé el archivo (Ctrl+S)?\n- [ ] ¿F5 en el navegador para actualizar?\n- [ ] ¿Abrí la consola (F12) para ver errores?\n- [ ] ¿Verifiqué que el HTML esté correcto?\n- [ ] ¿Las props se pasan correctamente?\n- [ ] ¿El estado se actualiza?\n- [ ] ¿Las funciones se llaman?\n\n---\n\n## 🚫 Errores comunes\n\n### \"hooks is not defined\"\n```javascript\n// ❌ Olvidaste importar\n// ✅ Agrega al inicio del archivo\nimport { useState } from 'react';\n```\n\n### \"Cannot read property 'map' of undefined\"\n```javascript\n// ❌ Tu array es null/undefined\nconst [videos, setVideos] = useState(null);\n\n// ✅ Inicia como array vacío\nconst [videos, setVideos] = useState([]);\n```\n\n### \"Cada elemento debe tener una key única\"\n```javascript\n// ❌ Falta key\n{items.map(item => <div>{item}</div>)}\n\n// ✅ Agrega key\n{items.map(item => <div key={item.id}>{item}</div>)}\n```\n\n---\n\n## 🎯 Patrones útiles\n\n### Cargar datos al montar\n```javascript\nuseEffect(() => {\n  const cargar = async () => {\n    const datos = await fetch(url);\n    setData(await datos.json());\n  };\n  cargar();\n}, []);\n```\n\n### Debounce (esperar a que deje de escribir)\n```javascript\nconst [busqueda, setBusqueda] = useState('');\n\nuseEffect(() => {\n  const timer = setTimeout(() => {\n    buscar(busqueda);\n  }, 500);\n  return () => clearTimeout(timer);\n}, [busqueda]);\n```\n\n### Toggle\n```javascript\nconst [abierto, setAbierto] = useState(false);\nconst toggle = () => setAbierto(!abierto);\n```\n\n---\n\n**Guarda esto para referencia rápida.** 💚\n"